#include <stdio.h>

int main() {
    char ch;

    // Take a single character as input
    printf();
    scanf("%c", &ch);

    // Print the character
    printf("You entered: %c\n", ch);

    return 0;
}
